import { Link } from "react-router-dom";

export function Header() {
    return (
        <header className="bg-teal-900 text-white flex items-center flex-col gap-3 py-4">
            <h1 className="text-2xl">Checkpoint : frontend</h1>
        </header>
    );
}
